Action Recognition Using Pose Estimation
------------------------------------------


Installation
------------
1. Make a virtual environment with python version being 3.7
2. Navigate to this folder and use the command 
	pip install -r requirements.txt
   
   This command will install the necessary packages for the project to run

3. Run the jupyter notebook cells in the same order as given in the notebook